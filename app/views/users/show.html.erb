<h1><%= @user.name %></h1>

<h2>Events</h2>
<ul>
<% @user.events.each do |event| %>
  <li>
    <%= event.name %>
  </li>
<% end %>
</ul>

<h2>Attended Events</h2>
<ul>
  <% @user.attended_events.each do |event| %>
    <li>
      <%= event.name %>
    </li>
  <% end %>
</ul>
